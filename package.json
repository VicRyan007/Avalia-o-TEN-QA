{
  "name": "avaliacao-qa-ten-serverest-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend exec playwright install",
    "test:api": "npm --prefix backend run test",
    "test:ui": "npm --prefix frontend run test",
    "test:all": "npm run test:api && npm run test:ui",
    "report:api": "npm --prefix backend run report",
    "report:ui": "npm --prefix frontend run report"
  }
}
